[Service]
# SELinux needs this process transition to apply the dedicated service domain.
# The domain, capabilities, namespace, device, and filesystem restrictions are
# the enforcement boundary; NoNewPrivileges would suppress the SELinux
# transition and leave the service in init_t.
NoNewPrivileges=false
RestrictSUIDSGID=false
